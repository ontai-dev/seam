---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: infrastructuretalosclusteroperationresults.infrastructure.ontai.dev
spec:
  group: infrastructure.ontai.dev
  names:
    kind: InfrastructureTalosClusterOperationResult
    listKind: InfrastructureTalosClusterOperationResultList
    plural: infrastructuretalosclusteroperationresults
    shortNames:
    - tcor
    singular: infrastructuretalosclusteroperationresult
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.capability
      name: Capability
      type: string
    - jsonPath: .spec.status
      name: Status
      type: string
    - jsonPath: .spec.clusterRef
      name: Cluster
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InfrastructureTalosClusterOperationResult is the immutable result record
          written by the Conductor execute-mode Job after a day-2 TalosCluster operation
          completes. One CR per Job, created in the Job's namespace (ont-system).
          Owned by the triggering platform day-2 CR via ownerReference for automatic GC.
          conductor-schema.md §8, seam-core-schema.md §TBD.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              InfrastructureTalosClusterOperationResultSpec is the complete result
              written by a Conductor execute-mode Job after a day-2 TalosCluster operation.
              Immutable after creation. One CR per Job. conductor-schema.md §8.
            properties:
              capability:
                description: Capability is the conductor capability that produced
                  this result.
                type: string
              clusterRef:
                description: ClusterRef is the name of the InfrastructureTalosCluster
                  this operation targeted.
                type: string
              completedAt:
                description: CompletedAt is the time the capability execution finished.
                format: date-time
                type: string
              failureReason:
                description: FailureReason is populated when Status is Failed. Nil
                  on success.
                properties:
                  category:
                    description: Category classifies the failure domain.
                    enum:
                    - ValidationFailure
                    - CapabilityUnavailable
                    - ExecutionFailure
                    - ExternalDependencyFailure
                    - InvariantViolation
                    type: string
                  reason:
                    description: Reason is a human-readable description of the failure.
                    type: string
                required:
                - category
                - reason
                type: object
              jobRef:
                description: JobRef is the Kubernetes Job name that produced this
                  result.
                type: string
              message:
                description: Message provides a human-readable summary of the outcome.
                type: string
              startedAt:
                description: StartedAt is the time the capability execution began.
                format: date-time
                type: string
              status:
                allOf:
                - enum:
                  - Succeeded
                  - Failed
                - enum:
                  - Succeeded
                  - Failed
                description: Status is the terminal status of the capability execution.
                type: string
            required:
            - capability
            - clusterRef
            - jobRef
            - status
            type: object
          status:
            description: |-
              InfrastructureTalosClusterOperationResultStatus is the observed state.
              Currently empty; reserved for future conditions.
            properties:
              observedGeneration:
                description: ObservedGeneration is the last generation observed by
                  any consumer.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
